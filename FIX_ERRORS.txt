================================================================================
   FIXES APPLIED - Please do these 3 simple steps
================================================================================

Your project had 3 errors. I have fixed them on disk.

STEP 1: Delete this file if it still exists (it causes a duplicate class error):
   C:\Users\sikke\Projects\TornadoShelterSim\resources\ItemResource.gd

   (I already tried to delete it. If it is still there, delete it manually.)

STEP 2: Double-click this file again:
   Launch TornadoShelterSim.bat

   Or open Godot and import the folder fresh.

STEP 3: If the scene still won't load, do this:
   - In Godot, go to FileSystem
   - Delete the file: scenes/world/Main.tscn
   - Then re-import or restart Godot
   (I already replaced it with a clean version)

After doing the above, the project should open without errors.

Controls:
   WASD = Move
   E    = Pick up items (when close)
   Tab  = Open Inventory

If you still get errors after this, copy the exact error message and send it to me.

================================================================================
